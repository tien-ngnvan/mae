from setuptools import setup, find_packages

setup(
    name='mae',
    # version='0.1',
    packages=find_packages('.'),
    install_requires=[
        # Add your dependencies here
    ],
    description='A mae package',
    python_requires='>=3.9',
)